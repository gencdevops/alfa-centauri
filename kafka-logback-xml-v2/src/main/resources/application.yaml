spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banka
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop

  kafka:
    bootstrap-servers:
      - localhost:9094