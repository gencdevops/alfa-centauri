# Read file on Config Server
# .properties or .yml file on config server
# local, dev
spring.profiles.active = dev
spring.application.name = cg-registry

# Config server setting
spring.cloud.config.uri = http://localhost:8888
